# coding: utf-8

"""
    Graphiant APIs

    Graphiant API documentation.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class AuthPermissions(BaseModel):
    """
    AuthPermissions
    """ # noqa: E501
    billing_and_invoicing: Optional[StrictStr] = Field(default=None, alias="billingAndInvoicing")
    licensing: Optional[StrictStr] = None
    order_status: Optional[StrictStr] = Field(default=None, alias="orderStatus")
    support: Optional[StrictStr] = None
    user_and_tenant_management: Optional[StrictStr] = Field(default=None, alias="userAndTenantManagement")
    asset_manager: Optional[StrictStr] = Field(default=None, alias="assetManager")
    global_services: Optional[StrictStr] = Field(default=None, alias="globalServices")
    network_configurations: Optional[StrictStr] = Field(default=None, alias="networkConfigurations")
    safety_and_security: Optional[StrictStr] = Field(default=None, alias="safetyAndSecurity")
    service_policies: Optional[StrictStr] = Field(default=None, alias="servicePolicies")
    compliance: Optional[StrictStr] = None
    developer_tools: Optional[StrictStr] = Field(default=None, alias="developerTools")
    insights: Optional[StrictStr] = None
    logs: Optional[StrictStr] = None
    monitoring_and_troubleshooting: Optional[StrictStr] = Field(default=None, alias="monitoringAndTroubleshooting")
    reports: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["billingAndInvoicing", "licensing", "orderStatus", "support", "userAndTenantManagement", "assetManager", "globalServices", "networkConfigurations", "safetyAndSecurity", "servicePolicies", "compliance", "developerTools", "insights", "logs", "monitoringAndTroubleshooting", "reports"]

    @field_validator('billing_and_invoicing')
    def billing_and_invoicing_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('licensing')
    def licensing_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('order_status')
    def order_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('support')
    def support_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('user_and_tenant_management')
    def user_and_tenant_management_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('asset_manager')
    def asset_manager_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('global_services')
    def global_services_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('network_configurations')
    def network_configurations_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('safety_and_security')
    def safety_and_security_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('service_policies')
    def service_policies_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('compliance')
    def compliance_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('developer_tools')
    def developer_tools_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('insights')
    def insights_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('logs')
    def logs_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('monitoring_and_troubleshooting')
    def monitoring_and_troubleshooting_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    @field_validator('reports')
    def reports_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS']):
            raise ValueError("must be one of enum values ('NO_ACCESS', 'READ_ONLY', 'FULL_ACCESS')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AuthPermissions from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AuthPermissions from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "billingAndInvoicing": obj.get("billingAndInvoicing"),
            "licensing": obj.get("licensing"),
            "orderStatus": obj.get("orderStatus"),
            "support": obj.get("support"),
            "userAndTenantManagement": obj.get("userAndTenantManagement"),
            "assetManager": obj.get("assetManager"),
            "globalServices": obj.get("globalServices"),
            "networkConfigurations": obj.get("networkConfigurations"),
            "safetyAndSecurity": obj.get("safetyAndSecurity"),
            "servicePolicies": obj.get("servicePolicies"),
            "compliance": obj.get("compliance"),
            "developerTools": obj.get("developerTools"),
            "insights": obj.get("insights"),
            "logs": obj.get("logs"),
            "monitoringAndTroubleshooting": obj.get("monitoringAndTroubleshooting"),
            "reports": obj.get("reports")
        })
        return _obj


